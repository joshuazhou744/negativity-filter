// background.js
// handles the background tasks

// reset the extension when the tab is updated
chrome.tabs.onUpdated.addListener((tabId, changeInfo, tab) => {
    // TODO: reset the extension when the tab is updated
});